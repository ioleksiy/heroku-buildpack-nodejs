#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -f $1/package.json ]; then
  echo "Node.js"
else
  echo "no" && exit 1
fi

if [ -z "$SSG_GIT_KEY" ]; then
  echo 'no' && exit 1;
else
  echo 'SSH' && exit 0
fi
